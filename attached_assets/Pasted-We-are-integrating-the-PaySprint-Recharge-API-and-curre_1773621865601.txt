We are integrating the PaySprint Recharge API and currently receiving this response from every endpoint (balance, recharge, operator list):

response_code: 6
message: "Invalid Credential"

Please debug the entire integration and verify the following items step by step.

1. JWT TOKEN

* Verify JWT is signed correctly using the provided JWT_KEY.
* Ensure the payload contains:

  * partnerId: "RUPYASETU"
  * timestamp (Unix epoch time in seconds)
  * reqid (unique request id)
* Log the decoded JWT payload to confirm correct structure.

2. REQUEST PAYLOAD
   Before encryption, confirm the payload structure is exactly:

{
"partnerId": "RUPYASETU",
"timestamp": <current_unix_timestamp>,
"reqid": "<unique_id>"
}

3. AES ENCRYPTION
   Verify that:

* AES algorithm is AES-256-CBC
* AES_ENCRYPTION_KEY and AES_ENCRYPTION_IV are used correctly
* Output is Base64 encoded (not hex)
* Log the encrypted output length.

4. REQUEST HEADERS
   Confirm the request headers are exactly:

Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

No "Authorisedkey" header should be included because we are using LIVE IP BASED authentication.

5. REQUEST BODY
   Ensure the API request body is structured as:

{
"data": "<encrypted_payload>"
}

6. API ENDPOINT
   Verify we are calling the correct PaySprint endpoint:

Balance API:
https://api.paysprint.in/service-api/api/v1/service/balance/balance

Recharge API:
https://api.paysprint.in/service-api/api/v1/service/recharge/recharge

7. SERVER IP CHECK
   Log the outgoing server IP using:
   curl ifconfig.me

Confirm the outbound IP is:
88.222.246.128

If it is different, the request may be rejected by PaySprint.

8. DEBUG LOGGING
   Add logs for:

* JWT payload
* Raw request payload
* Encrypted payload
* Request headers
* API response

Print the full response body from PaySprint.

9. TEST WITH BALANCE API FIRST
   Before recharge, test only the balance endpoint to confirm credentials work.

Return a detailed debug report showing which step fails.
