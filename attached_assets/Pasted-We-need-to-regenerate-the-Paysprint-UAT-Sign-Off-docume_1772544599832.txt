We need to regenerate the Paysprint UAT Sign-Off document strictly based on the credential panel provided by Paysprint.

Do NOT assume JWT-based authentication unless explicitly enabled.
Use only the following credential information:

VERSION: IP AND AUTHORIZED KEY BASED
ENVIRONMENT: UAT
BASE URL: https://sit.paysprint.in/service-api/api/v1
AUTHORISED KEY: (masked in document)
JWT TOKEN: (masked, do not assume signing secret)
AES ENCRYPTION KEY: (masked)
AES ENCRYPTION IV: (masked)
SERVER IP: 34.41.220.14

Follow these rules strictly:

1. Authentication Section:
   - Clearly state: Authentication Model = IP Whitelisting + Authorisedkey Header
   - Do NOT state that account is JWT-based.
   - Mention Token header is sent as per API documentation.
   - Do NOT claim signing secret is verified.

2. Remove all experimental signing secret attempts.
   - Do NOT mention decoded partner key.
   - Do NOT mention multiple JWT secret attempts.
   - Keep document clean and professional.

3. Include real UAT API logs only:
   - browseplan
   - dorecharge
   - status
   - Include HTTP status and raw JSON response
   - Mask sensitive values

4. Mention clearly:
   - "As per credential panel, account version is IP AND AUTHORIZED KEY BASED."
   - "Awaiting confirmation from Paysprint if additional JWT signing secret is required for SIT."

5. Keep tone neutral.
   - Do NOT write that integration is fully verified.
   - Do NOT write that Paysprint is incorrect.
   - Use professional language.

6. Document Structure:
   - Project Details
   - Authentication Mechanism
   - Encryption Details
   - API Endpoints
   - UAT Logs (with timestamps)
   - API Call Summary Table
   - Verification Checklist
   - Action Items for Paysprint
   - Credentials Summary (masked)
   - Sign-Off Section

7. Replace:
   "Mode: LIVE API CALL"
   with
   "Mode: UAT API CALL (SIT Environment)"

8. Version the document as:
   Document Ver: 7.0

9. Do NOT fabricate success responses.
   Include actual runtime responses only.

Generate a clean, professional markdown document ready to attach in email.